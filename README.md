# educk-theme

educk-theme is a WordPress theme for [educk.pl](https://educk.pl) and [educk.org](https://educk.org).

Startup commands before coding:
- `cd /Users/jantuziak/Sites/educk-local && php -S localhost:8000` for starting the WordPress site
- `npm run dev` for starting the Tailwind watcher